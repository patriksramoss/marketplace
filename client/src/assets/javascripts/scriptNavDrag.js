import * as $ from "jquery";
import Draggable from "https://cdn.jsdelivr.net/npm/@shopify/draggable/build/esm/Draggable/Draggable.mjs";

export function NavDrag() {
  // var nav = Draggable.create(".mobile-nav", {
  //   cursor: "pointer",
  //   bounds: ".nav-bound",
  // });
}
